<div class="comment-form">
  <%= form_with(model: [ @post, @post.comments.build ]) do |f| %>
  <div class="form-group">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <%= f.submit %>
  <% end %>
</div>
